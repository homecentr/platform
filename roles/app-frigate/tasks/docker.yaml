- name: Create compose file
  ansible.builtin.copy:
    dest: /etc/frigate/docker-compose.yaml
    force: true
    content: "{{ lookup('template', 'docker-compose.j2') }}"

- name: Start docker stack
  community.docker.docker_compose_v2:
    project_src: /etc/frigate
    state: "{{ 'restarted' if app_frigate_config.changed or app_homepage_icons.changed else 'present' }}"
