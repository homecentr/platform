exclude_paths:
  - .cache/
  - ${PWD}/.github/
  - "${PWD}/environments/*/gluster_nodes/ca.yml"
  - "${PWD}/**/*.sops.yml"

use_default_rules: true

skip_list:
  - role-name[path]
  - name[play]
  - yaml[octal-values]

offline: false
