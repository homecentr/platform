{
  "name": "@homecentr/platform",
  "version": "1.0.0",
  "repository": "git@github.com:homecentr/platform.git",
  "author": "Lukas Holota",
  "license": "GPL",
  "private": true,
  "scripts": {
    "install": "./tools/install.sh",
    "lint": "ANSIBLE_CONFIG=\"./ansible.cfg\" ansible-lint",
    "init:pve": "ANSIBLE_HOST_KEY_CHECKING=False ./tools/apply.sh pve-nodes -u root -e ansible_user=root --tags init -k",
    "apply": "./tools/apply.sh"
  },
  "devDependencies": {
    "@homecentr/yaml-diff": "1.1.7",
    "dotenv": "^16.4.5",
    "env-cmd": "^10.1.0",
    "nightwatch": "^3.6.1"
  },
  "dependencies": {
    "nodemailer": "^6.9.13"
  }
}
