all:
  children:
    pve_nodes:
      hosts:
        pve1:
        pve2:
        pve3:
    app_containers:
      children:
        # app_coredns:
        #   hosts:
        #     app_coredns_primary:
        #     app_coredns_secondary:
        app_pihole:
          hosts:
            app_pihole_single:
        app_pomerium:
          hosts:
            app_pomerium_primary:
            app_pomerium_secondary:
        app_mqtt:
          hosts:
            app_mqtt_single: